# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...except the key-names-only template, which is meant to be committed.
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts
.vercel

# eve dev runtime artifacts
.eve

# e2e failure artifacts (console dumps, screenshots)
/tests/e2e/artifacts/
